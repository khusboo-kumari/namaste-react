// import { useState } from "react";
import React from "react";

const User = ({ name }) => {
  // const [count, setCount] = useState(0);

  // const incrementCount = () => {
  //   setCount(count + 1);
  // };
  return (
    <div className="user-card">
      {/* <h1>Count = {count}</h1> */} 

      {/* <h2>Name: {name}</h2> */}
     
      {/* <button onClick={incrementCount}>
        See how the Count is increased .{" "}
      </button> */}
    </div>
  );
};

export default User;
